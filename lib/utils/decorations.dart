import 'package:flutter/material.dart';
import 'utils.dart';

class LanceDecorations {
  static const formStyle = InputDecoration();

  static const buttonStyle = BoxDecoration(
    borderRadius: BorderRadius.all(
      Radius.circular(16),
    ),
    color: LanceColors.primaryColor,
  );
}
